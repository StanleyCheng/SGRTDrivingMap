name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Verify source
        run: npm run verify
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5
      - name: Static export build
        # The base path comes from Pages itself, so renaming the repository can
        # never leave the deploy serving assets from a path that no longer exists.
        env:
          BASE_PATH: ${{ steps.pages.outputs.base_path }}
        run: npm run build:static
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "out"
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        id: deployment
